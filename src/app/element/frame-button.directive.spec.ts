import { FrameButtonDirective } from './frame-button.directive';

describe('FrameButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FrameButtonDirective();
    expect(directive).toBeTruthy();
  });
});
